machine:
  environment:
    CONTAINER_NAME: quay.io/redsift/sandbox-clojure
    CLOJURE_VERSIONS: 1.8.0
  pre:
    - sudo curl -L -o /usr/bin/docker 'https://s3-external-1.amazonaws.com/circle-downloads/docker-1.9.0-circleci'
    - sudo chmod 0755 /usr/bin/docker
  services:
    - docker

dependencies:
  override:
    - docker info
    - |
      set -e
      for CV in $CLOJURE_VERSIONS
      do
        docker build --build-arg version=$CV -t $CV .
      done

test:
  override:
    - |
      set -e
      for CV in $CLOJURE_VERSIONS
      do
        docker run -t --entrypoint=lein $CV -version
        INIT=$(docker inspect -f "{{index .ContainerConfig.Labels \"io.redsift.sandbox.install\" }}" $CV)
        echo "Calling init = $INIT"
        docker run -v $PWD/test:/run/dagger/sift $CV $INIT 0 1 2
        ls -A $PWD/test/server/classes || true
        RUN=$(docker inspect -f "{{index .ContainerConfig.Labels \"io.redsift.sandbox.run\" }}" $CV)
        echo "Calling run = $RUN"
        docker run -v $PWD/test:/run/dagger/sift -e DRY=true $CV $RUN 0 1 2
        sudo rm -Rf $PWD/test/server/classes || true
      done

deployment:
  production:
    branch: master
    commands:
      - docker login -e $QUAY_EMAIL -u $QUAY_USER -p $QUAY_PASS quay.io
      - |
        set -e
        for CV in $CLOJURE_VERSIONS
        do
          VERSION=v${CV}
          MAJOR_NUMBER=(${VERSION//./ })
          MAJOR_VERSION=$MAJOR_NUMBER
          echo "Tagging as $CONTAINER_NAME:$VERSION"
          docker tag $CV:latest $CONTAINER_NAME:$VERSION
          echo "Tagging as $CONTAINER_NAME:$MAJOR_VERSION"
          docker tag $CV:latest $CONTAINER_NAME:$MAJOR_VERSION
        done
        docker push $CONTAINER_NAME
